# ☽ MoonLight — Management Panel

Веб-панель управления для Discord сообщества MoonLight.

---

## 🚀 Деплой на Replit

### 1. Загрузи проект
- Загрузи все файлы в Replit или подключи GitHub репозиторий

### 2. Установи Secrets в Replit
Перейди в **Secrets** (замок слева) и добавь:

| Key | Value |
|-----|-------|
| `SECRET_KEY` | любая случайная строка (например: `moonlight_super_secret_2024`) |
| `DISCORD_TOKEN` | токен твоего Discord бота |
| `DISCORD_CLIENT_ID` | Client ID из Discord Developer Portal |
| `DISCORD_CLIENT_SECRET` | Client Secret из Discord Developer Portal |
| `DISCORD_REDIRECT_URI` | `https://ТВО-REPLIT-ДОМЕН.repl.co/auth/discord/callback` |
| `GUILD_ID` | `1062306150365794364` |

### 3. Настрой Discord OAuth2
1. Зайди на [discord.com/developers](https://discord.com/developers)
2. Выбери своё приложение (или создай новое)
3. **OAuth2 → Redirects** → добавь: `https://твой-домен.repl.co/auth/discord/callback`
4. Скопируй Client ID и Client Secret в Secrets

### 4. Запуск
Нажми **Run** — сайт запустится автоматически.

---

## 🔐 Роли

| Роль | Что видит |
|------|-----------|
| **Admin** | Всё: пользователи, сброс паролей, смена ролей, собрания, переводы |
| **Owner** | Пользователи (без смены ролей и сброса паролей), собрания, переводы |
| **Newbie** | Только предстоящие собрания, подтверждение участия |

**Дефолтный аккаунт:**
- Логин: `admin`
- Пароль: `admin123`
- ⚠️ Сразу смени пароль!

---

## 📋 Функционал

### Собрания
- Admin/Owner создают собрание → бот отправляет embed в Discord канал
- Newbie видит собрания → нажимает открыть → вводит @discord_username
- Кнопки **Приду** / **Не приду** (не работают без валидного Discord)
- При «Не приду» — вводит причину → Admin/Owner одобряют/отклоняют
- При отклонении — бот шлёт DM пользователю, он помечается как **Бездарь** в статистике

### Переводы
- Создание перевода с организацией, получателем, причиной
- Статусы: ожидает / одобрен / отклонён
- Real-time обновления через WebSocket

### Пользователи
- Регистрация через логин+пароль или Discord OAuth2
- Новые пользователи ждут подтверждения от Admin/Owner
- Admin может сбросить пароль (новый пароль отправляется в Discord DM)
- Admin может менять роли

---

## 📁 Структура

```
moonlight/
├── app.py              # Основное Flask приложение
├── requirements.txt    # Зависимости
├── .env.example        # Пример переменных окружения
├── .replit             # Конфиг Replit
├── database/           # SQLite база (создаётся автоматически)
│   └── moonlight.db
└── templates/          # HTML шаблоны
    ├── base.html           # Базовый layout (sidebar, navbar)
    ├── login.html          # Страница входа/регистрации
    ├── dashboard.html      # Главная панель
    ├── meetings.html       # Список собраний
    ├── meeting_detail.html # Детали собрания + ответ
    ├── create_meeting.html # Создание собрания
    ├── transfers.html      # Переводы
    ├── users.html          # Управление пользователями
    ├── 403.html
    └── 404.html
```
